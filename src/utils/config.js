import { Dimensions } from 'react-native'

const width = Dimensions.get('window').width

export default {
  width,
  padding: 20,
  cols: 4,
  spaceHorizontal: 10,
  spaceVertical: 15,
  debug: false
}
